{
  "name": "default",
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "admin",
  "database": "fastforward_task_manager",
  "synchronize": true,
  "logging": false,
  "entities": [
    "${rootDir}/entities/**/*.{js,ts}"
  ],
  "migrations": [
    "${rootDir}/migration/**/*.{js,ts}"
  ],
  "subscribers": [
    "${rootDir}/subscriber/**/*.{js,ts}"
  ],
  "cli": {
    "entitiesDir": "${rootDir}/entity",
    "migrationsDir": "${rootDir}/migration",
    "subscribersDir": "${rootDir}/subscriber"
  }
}